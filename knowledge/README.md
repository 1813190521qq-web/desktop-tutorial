# Knowledge Modules

Knowledge modules are versioned reference packages that give an agent stable domain context without embedding that context directly in application code.

Each module should state its scope, source or ownership, update cadence, and any safety limits. Keep private data, credentials, and unverified claims out of this directory.

The first modules will cover workflow design patterns, tool-use boundaries, and evaluation practices for agent builders.

